mybatis:
  config-location: classpath:mybatis/mybatis-config.xml
  mapper-locations: classpath:mybatis/mapper/*.xml
  type-aliases-package: com.cs.cstools.entity

spring:
  jpa:
    show-sql: true
  profiles:
    active: dev

logging:
  level:
    com.cs.cstools.mapper: debug

